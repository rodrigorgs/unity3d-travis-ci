using UnityEngine;
using System.Collections;

public class Init : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Physics.gravity.Set (0, -5.0f, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
